// 8. Peça se o usuário aceitou os termos (sim/não) e confirmou o 
// email (sim/não). Exiba "Cadastro completo" se ambos forem "sim"

let confirma = prompt("Para aceirta o termo sim ou não ")
    //confirma = "sim"
if (confirma ==="sim"){
   alert("aceitou o termo")
    }else {
    alert ("Escreva sim para aceitar o termo")}
